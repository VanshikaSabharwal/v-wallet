{
  "$schema": "https://turbo.build/schema.json",

  "pipeline": {
    "build": {
      "commands": ["npx turbo run build"],
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"]
    },

    "lint": {},
    "dev": {
      "cache": false
    }
  }
}
