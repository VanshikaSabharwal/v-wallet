{
  "$schema": "https://turbo.build/schema.json",
  "scripts": {
    "build": "turbo run build --filter=web"
  },
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"]
    },
    "lint": {},
    "dev": {
      "cache": false
    }
  }
}
